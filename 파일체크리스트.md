# 📋 파일 생성 체크리스트

## ✅ 백엔드 파일 (backend/)

### 필수 파일

-   [ ] `server.py` - Flask 서버 (RAG 통합 버전)
-   [ ] `rag_manager.py` - RAG 시스템 관리자
-   [ ] `requirements.txt` - Python 패키지 목록

### 자동 생성 폴더

-   [ ] `venv/` - Python 가상환경 (자동)
-   [ ] `uploads/` - 업로드 파일 저장소 (자동)
-   [ ] `chroma_db/` - ChromaDB 벡터 DB (자동)

---

## ✅ 프론트엔드 파일 (frontend/)

### 설정 파일

-   [ ] `package.json` - npm 패키지 (RxJS 포함)
-   [ ] `tsconfig.json` - TypeScript 설정
-   [ ] `next.config.ts` - Next.js 설정
-   [ ] `tailwind.config.ts` - Tailwind CSS 설정
-   [ ] `.env.local` - 환경 변수

### App 디렉토리 (src/app/)

-   [ ] `layout.tsx` - 루트 레이아웃
-   [ ] `page.tsx` - 메인 페이지 (RxJS 버전)
-   [ ] `globals.css` - 전역 스타일

### 컴포넌트 (src/components/)

-   [ ] `Header.tsx` - 헤더 (통계 버튼 포함)
-   [ ] `ChatMessage.tsx` - 메시지 컴포넌트
-   [ ] `ChatInput.tsx` - 입력 컴포넌트
-   [ ] `QuickButtons.tsx` - 빠른 질문 버튼
-   [ ] `TypingIndicator.tsx` - 타이핑 인디케이터
-   [ ] `DocumentUpload.tsx` - 파일 업로드 (RxJS 버전)
-   [ ] `DocumentStats.tsx` - 문서 통계 모달

### 서비스 (src/services/)

-   [ ] `api.service.ts` - RxJS 기반 API 서비스

### 상태 관리 (src/store/)

-   [ ] `chatStore.ts` - Jotai Atoms + RxJS Subjects

### 훅 (src/hooks/)

-   [ ] `useChatController.ts` - RxJS 통합 컨트롤러

### 타입 정의 (src/types/)

-   [ ] `chat.ts` - TypeScript 인터페이스

---

## 📝 파일 생성 순서 (추천)

### 1단계: 프로젝트 구조 생성

```bash
mkdir elementary-chatbot
cd elementary-chatbot
mkdir -p backend frontend
```

### 2단계: 백엔드 기본 설정

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
```

### 3단계: 백엔드 파일 생성

1. `requirements.txt` 생성 및 설치
2. `rag_manager.py` 생성
3. `server.py` 생성

### 4단계: 프론트엔드 프로젝트 생성

```bash
cd ../
npx create-next-app@15.5.7 frontend --typescript --tailwind --app
cd frontend
npm install jotai rxjs framer-motion lucide-react react-textarea-autosize
```

### 5단계: 프론트엔드 디렉토리 생성

```bash
mkdir -p src/{services,store,hooks,types}
```

### 6단계: 프론트엔드 파일 생성 (순서대로)

1. `src/types/chat.ts` (타입 먼저)
2. `src/services/api.service.ts` (API 서비스)
3. `src/store/chatStore.ts` (상태 관리)
4. `src/hooks/useChatController.ts` (Hook)
5. `src/components/*.tsx` (컴포넌트들)
6. `src/app/globals.css` (스타일)
7. `src/app/layout.tsx` (레이아웃)
8. `src/app/page.tsx` (메인 페이지)

### 7단계: 환경 변수 설정

```bash
echo "NEXT_PUBLIC_API_URL=http://localhost:5000" > .env.local
```

---

## 🎯 각 파일의 역할

### 백엔드

| 파일               | 역할        | 의존성                          |
| ------------------ | ----------- | ------------------------------- |
| `requirements.txt` | 패키지 목록 | -                               |
| `rag_manager.py`   | RAG 시스템  | ChromaDB, Sentence Transformers |
| `server.py`        | Flask 서버  | rag_manager.py                  |

### 프론트엔드 (생성 순서)

| 순서 | 파일                   | 역할      | 의존성                        |
| ---- | ---------------------- | --------- | ----------------------------- |
| 1    | `chat.ts`              | 타입 정의 | -                             |
| 2    | `api.service.ts`       | API 통신  | RxJS, chat.ts                 |
| 3    | `chatStore.ts`         | 상태 관리 | Jotai, RxJS, chat.ts          |
| 4    | `useChatController.ts` | 컨트롤러  | api.service.ts, chatStore.ts  |
| 5    | `TypingIndicator.tsx`  | UI        | Framer Motion                 |
| 6    | `ChatMessage.tsx`      | UI        | chat.ts, Framer Motion        |
| 7    | `ChatInput.tsx`        | UI        | Framer Motion, Lucide         |
| 8    | `QuickButtons.tsx`     | UI        | Framer Motion, Lucide         |
| 9    | `DocumentUpload.tsx`   | UI        | api.service.ts, chatStore.ts  |
| 10   | `DocumentStats.tsx`    | UI        | chatStore.ts                  |
| 11   | `Header.tsx`           | UI        | DocumentUpload, DocumentStats |
| 12   | `globals.css`          | 스타일    | -                             |
| 13   | `layout.tsx`           | 레이아웃  | globals.css                   |
| 14   | `page.tsx`             | 메인      | 모든 컴포넌트                 |

---

## 🔍 파일 검증

### 백엔드 검증

```bash
cd backend
source venv/bin/activate

# 파일 존재 확인
ls -la server.py rag_manager.py requirements.txt

# Python 문법 체크
python -m py_compile server.py
python -m py_compile rag_manager.py
```

### 프론트엔드 검증

```bash
cd frontend

# 파일 존재 확인
ls -la src/services/api.service.ts
ls -la src/store/chatStore.ts
ls -la src/hooks/useChatController.ts
ls -la src/types/chat.ts

# TypeScript 컴파일 체크
npm run build
```

---

## 📦 Artifact에서 복사할 파일 목록

### 백엔드 (3개)

1. `requirements.txt` → `backend/requirements.txt`
2. `rag_manager.py` → `backend/rag_manager.py`
3. `server.py (RAG 통합)` → `backend/server.py`

### 프론트엔드 (15개)

1. `package.json` → `frontend/package.json`
2. `chat.ts` → `frontend/src/types/chat.ts`
3. `api.service.ts` → `frontend/src/services/api.service.ts`
4. `chatStore.ts` → `frontend/src/store/chatStore.ts`
5. `useChatController.ts` → `frontend/src/hooks/useChatController.ts`
6. `Header.tsx` → `frontend/src/components/Header.tsx`
7. `ChatMessage.tsx` → `frontend/src/components/ChatMessage.tsx`
8. `ChatInput.tsx` → `frontend/src/components/ChatInput.tsx`
9. `QuickButtons.tsx` → `frontend/src/components/QuickButtons.tsx`
10. `TypingIndicator.tsx` → `frontend/src/components/TypingIndicator.tsx`
11. `DocumentUpload.tsx (RxJS)` → `frontend/src/components/DocumentUpload.tsx`
12. `DocumentStats.tsx` → `frontend/src/components/DocumentStats.tsx`
13. `page.tsx (RxJS)` → `frontend/src/app/page.tsx`
14. `layout.tsx` → `frontend/src/app/layout.tsx`
15. `globals.css` → `frontend/src/app/globals.css`

### 설정 파일 (4개)

1. `tsconfig.json` → `frontend/tsconfig.json`
2. `next.config.ts` → `frontend/next.config.ts`
3. `tailwind.config.ts` → `frontend/tailwind.config.ts`
4. `.env.example` → `frontend/.env.local`

---

## ✨ 완료 확인

모든 파일 생성 후:

```bash
# 백엔드 테스트
cd backend
source venv/bin/activate
python server.py

# 프론트엔드 테스트 (새 터미널)
cd frontend
npm run dev
```

정상 작동하면 ✅ 완료!
